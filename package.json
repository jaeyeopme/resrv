{
  "name": "resrv",
  "version": "0.0.1",
  "private": true,
  "description": "Reservation API repository tooling",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "commitlint": "commitlint --last --verbose",
    "hooks:install": "lefthook install"
  },
  "devDependencies": {
    "@commitlint/cli": "21.0.0",
    "@commitlint/config-conventional": "21.0.0",
    "lefthook": "2.1.6"
  }
}
